一、程序的组成
1、main()函数
在所有的可执行的C语言程序中，唯一必不可少的部分是main()函数。通常情况下，程序从main函数中的第一条语句开始执行，到main函数中的最后一条语句结束。
2、#include指令
指令#include，在编译时将一个包含文件内容添加到程序中。包含文件的扩展名总是.h(如studio.h)
3、变量
是给用于存储信息的内存单元赋于名称。在程序执行期间，程序使用变量来存储各种信息。在C语言中，使用变量前必须定义它。
4、函数原型
5、程序语句
6、函数定义
7、程序注释
二、